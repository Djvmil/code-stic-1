#include<stdio.h>
#include<math.h>

int main(){
    int a,b,pgcd,i;
        printf("veuiller saisir les deux valeurs"); scanf("%d%d",&a,&b);
           do{ if(a>b){
                pgcd1=a-b;
                        }

                if(a<b){
                  pgcd2=b-a;
                        }



           }    while(a!=b);

system("pause");
getch();
return 0;
}
